apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "smart-env-monitor.fullname" . }}-config
  labels:
    {{- include "smart-env-monitor.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.env }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
